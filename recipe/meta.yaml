{% set name = "frozendict" %}
{% set version = "2.0.5" %}
{% set sha256 = "c1bee1c070d8d9f640e128e5b87576a4400086009f7862d13c0bf8600e6213d3" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . --no-deps -vv

requirements:
  host:
    - pip
    - python
  run:
    - python

test:
  imports:
    - frozendict

about:
  home: https://github.com/slezica/python-frozendict
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: An immutable dictionary
  dev_url: https://github.com/slezica/python-frozendict
